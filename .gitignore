# dependencies
node_modules/

# build output
dist/
.astro/
.vercel/

# local SQLite transcript store
data/

# env — secrets never committed
.env
.env.*
!.env.example

# logs / os
*.log
.DS_Store
.vercel

# skill-registry local cache (machine-local fingerprint, not content)
.atl/.skill-registry.cache.json
