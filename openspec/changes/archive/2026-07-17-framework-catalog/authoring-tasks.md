# C3 Framework Catalog — Pending Authoring Tasks

Generated by the `sdd-apply` phase after seeding the global catalog from
`docs/app_description/02-domain/framework/`. These items are tracked in
`framework_gaps` (DB) and listed here for human visibility.

All items have `status = pending_authoring` in the DB gap log.

---

## 1. Missing BARS file — SRX role

| Field | Value |
|---|---|
| kind | `role_no_bars` |
| role_code | `SRX` |
| competency_code | null |
| status | `pending_authoring` |

SRX has 18 assigned competencies but no `docs/app_description/02-domain/framework/bars/SRX.json`
file. All 18 competencies have `bars_available=false` on the API. A gap is also recorded
for the missing `responsibilities` field (`missing_role_meta`).

**Action**: author `bars/SRX.json` with 18 competency entries × N behavioral indicators each,
plus `responsibilities` text for the SRX role descriptor.

---

## 2. Missing role metadata — SRX responsibilities

| Field | Value |
|---|---|
| kind | `missing_role_meta` |
| role_code | `SRX` |
| competency_code | null |
| status | `pending_authoring` |

`roles.json` includes SRX but the `responsibilities` field is an empty string.

**Action**: add SRX responsibilities text to `roles.json` under the `responsibilities` key.

---

## 3. Missing BARS competencies — FLL (10 gaps)

| Field | Value |
|---|---|
| kind | `competency_no_bars` |
| role_code | `FLL` |
| competency_code | (one row per absent code) |
| status | `pending_authoring` |

FLL has 18 assigned competencies but `bars/FLL.json` covers only 8
(STG, INN, CSF, OPX, INS, INF, RES, LRN). The remaining 10 are unresolved.

**Action**: extend `bars/FLL.json` with the 10 missing competencies:
PRS, JDG, DRV, SLF, TMG, COM, COL, NET, ITG, INC.

---

## 4. Missing BARS competencies — MLL (10 gaps)

| Field | Value |
|---|---|
| kind | `competency_no_bars` |
| role_code | `MLL` |
| competency_code | (one row per absent code) |
| status | `pending_authoring` |

MLL has 18 assigned competencies but `bars/MLL.json` covers only 8
(same 8 as FLL). The remaining 10 are unresolved.

**Action**: extend `bars/MLL.json` with the 10 missing competencies.

---

## 5. Missing BARS competencies — BUL (6 gaps)

| Field | Value |
|---|---|
| kind | `competency_no_bars` |
| role_code | `BUL` |
| competency_code | (one row per absent code) |
| status | `pending_authoring` |

BUL has 14 assigned competencies but `bars/BUL.json` covers only 8
(same 8). The remaining 6 are unresolved.

**Action**: extend `bars/BUL.json` with the 6 missing competencies.

---

## 6. Missing potential competencies — MTG and LAT

| Field | Value |
|---|---|
| kind | `missing_potential_competency` |
| competency_code | `MTG` / `LAT` |
| status | `pending_authoring` |

The domain spec defines MTG (Managing Transition) and LAT (Leading Adaptive Teams)
as the two competencies for `assessment_type=potential`. Neither appears in
`competencies.json` or any BARS file yet.

**Action**: add MTG and LAT entries to `competencies.json` with `"type": "potential"`
and create their BARS anchors.

---

## 7. Missing Italian (IT) translations — global

| Field | Value |
|---|---|
| kind | `missing_translation` |
| role_code | null |
| competency_code | null |
| note | `it locale not yet authored` |
| status | `pending_authoring` |

All seeded roles, competencies, and BARS indicators have English (EN) translations only.
Italian content has NOT been authored. Every `BarsIndicatorResource` will return
`translation_gap=true` until IT translations are provided.

**Action**: author IT translations for:
- `roles.json`: `name` and `responsibilities` for all 5 roles
- `competencies.json`: `name` and `definition` for all 18 (+ MTG/LAT once authored)
- `bars/{ROLE}.json`: `indicator`, `scale.5`, `scale.3`, `scale.1` for every indicator entry

Once authored, re-run `php artisan db:seed --class=FrameworkCatalogSeeder`. The seeder
preserves existing translations and adds IT values via `setTranslation`.

---

## Summary

| Gap type | Count |
|---|---|
| `role_no_bars` | 1 (SRX) |
| `missing_role_meta` | 1 (SRX responsibilities) |
| `competency_no_bars` | 26 (FLL 10 + MLL 10 + BUL 6) |
| `missing_potential_competency` | 2 (MTG, LAT) |
| `missing_translation` | 1 (global IT locale) |
| **Total** | **31** |
