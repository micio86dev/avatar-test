# Delta for Scoring Model (Change Artifact — Archived)

> This delta has been merged into the promoted source-of-truth at:
> `openspec/specs/scoring-model/spec.md`

The "Reliability Formula and Valid-Competency Predicate" requirement (R-A assessable-fraction
+ V-A T=50% default) has replaced the old "Reliability Is a Separate Value — Formula Out of Scope"
requirement in the base spec. See `openspec/specs/scoring-model/spec.md` for the current
authoritative spec.

This file is preserved here as the audit trail of the delta applied during this change.
